# Content Approval 📅 (Marketing)

When a new content draft appears, it **summarises** it (platform, format, date, caption) and asks before anything is published.
It always asks first — nothing goes public without your YES, and **you** approve the publish. It never posts on its own.
Copy `agents/_template`, fill from this `my-agent.md`, add one line to `agents/registry.ts`.
