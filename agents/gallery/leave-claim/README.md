# Leave / Claim Approval 🗂️ (HR)

When staff submit leave or an expense claim, it **lays it out** (who, dates/amount, balance) for a one-glance decision.
It always asks — HR sign-off never runs on its own, and **you** approve or reject. It never auto-approves or pays.
Copy `agents/_template`, fill from this `my-agent.md`, add one line to `agents/registry.ts`.
